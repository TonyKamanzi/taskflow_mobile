import { ScrollView, Text, View } from "react-native";


export default function settings() {
    return (
        <ScrollView className="dark:bg-[#0f0f1e]">
            <Text className="dark:text-white text-center mt-12 text-4xl">settings</Text>
        </ScrollView>
    )
}
